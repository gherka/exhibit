from setuptools import setup, find_packages

setup(name='demonstrator',
      version='0.1',
      description='Command line tool for generating demonstrator data',
      author='German PRiks',
      packages=find_packages(),
     )
